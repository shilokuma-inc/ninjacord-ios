# ビルドフェーズの SwiftLint は $SRCROOT で引数なしに実行されるため、
# リポジトリ配下を丸ごと再帰的に lint する。ビルド生成物を除外しておかないと、
# リポジトリ内に DerivedData を作った際に SPM 依存のソースまで対象になり、
# 依存ライブラリ由来の violation でビルドが落ちる。
# ルールは既定のまま（本ファイルは除外設定のみ）。
excluded:
  - DerivedData
  - .build
